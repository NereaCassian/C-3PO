name: Build Extension

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read
      issues: read
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history for changelog generation
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build extension for Chrome
      run: npm run build:chrome
      
    - name: Build extension for Firefox
      run: npm run build:firefox
      
    - name: Check build output
      run: |
        echo "📁 Checking build output..."
        if [ -d "dist" ]; then
          echo "✅ dist directory exists"
          ls -la dist/
        else
          echo "❌ dist directory not found"
          exit 1
        fi
        
        if [ -d "public" ]; then
          echo "✅ public directory exists"
          ls -la public/
        else
          echo "❌ public directory not found"
          exit 1
        fi
        
        if [ -f "public/manifest.json" ]; then
          echo "✅ manifest.json found in public"
        elif [ -f "manifest.json" ]; then
          echo "✅ manifest.json found in root"
        else
          echo "❌ manifest.json not found"
          exit 1
        fi
        
        # Check for required extension files
        echo "🔍 Checking for required extension files..."
        required_files=("manifest.json" "popup.html" "popup.js")
        for file in "${required_files[@]}"; do
          if [ -f "dist/$file" ] || [ -f "public/$file" ]; then
            echo "✅ $file found"
          else
            echo "⚠️  $file not found in dist or public"
          fi
        done
      
    - name: Build Chrome extension
      run: |
        echo "📦 Building Chrome extension..."
        npm run build:chrome
        
        # Copy Chrome build to separate directory
        mkdir -p dist-chrome
        cp -r dist/* dist-chrome/
        
        echo "✅ Chrome build ready in dist-chrome/"
        ls -la dist-chrome/
        
    - name: Build Firefox extension  
      run: |
        echo "📦 Building Firefox extension..."
        npm run build:firefox
        
        # Copy Firefox build to separate directory
        mkdir -p dist-firefox
        cp -r dist/* dist-firefox/
        
        echo "✅ Firefox build ready in dist-firefox/"
        ls -la dist-firefox/
        
    - name: Verify builds
      run: |
        echo "📋 Verifying extension builds..."
        
        # Verify Chrome build
        echo "🔍 Chrome build contents:"
        ls -la dist-chrome/
        
        # Verify Firefox build  
        echo "🔍 Firefox build contents:"
        ls -la dist-firefox/
        
        # Check that both have manifest.json at root
        if [ -f "dist-chrome/manifest.json" ]; then
          echo "✅ Chrome: manifest.json found"
          echo "Chrome manifest background:"
          grep -A 3 '"background"' dist-chrome/manifest.json || true
        else
          echo "❌ Chrome: manifest.json not found"
          exit 1
        fi
        
        if [ -f "dist-firefox/manifest.json" ]; then
          echo "✅ Firefox: manifest.json found"
          echo "Firefox manifest background:"
          grep -A 3 '"background"' dist-firefox/manifest.json || true
        else
          echo "❌ Firefox: manifest.json not found"
          exit 1
        fi
        
    - name: Upload Chrome extension
      id: upload-chrome
      uses: actions/upload-artifact@v4
      with:
        name: C-3PO-chrome-v${{ github.run_number }}
        path: dist-chrome/
        retention-days: 30
        compression-level: 6  # Default compression for directory
        if-no-files-found: error
        overwrite: false
        
    - name: Upload Firefox extension
      id: upload-firefox  
      uses: actions/upload-artifact@v4
      with:
        name: C-3PO-firefox-v${{ github.run_number }}
        path: dist-firefox/
        retention-days: 30
        compression-level: 6  # Default compression for directory
        if-no-files-found: error
        overwrite: false
        
    - name: Verify uploads
      run: |
        echo "✅ Extensions uploaded successfully!"
        echo "📦 Chrome Artifact ID: ${{ steps.upload-chrome.outputs.artifact-id }}"
        echo "📦 Firefox Artifact ID: ${{ steps.upload-firefox.outputs.artifact-id }}"
        echo "🔗 Chrome Download URL: ${{ steps.upload-chrome.outputs.artifact-url }}"
        echo "🔗 Firefox Download URL: ${{ steps.upload-firefox.outputs.artifact-url }}"
        
    - name: Generate AI Changelog
      if: github.ref == 'refs/heads/main'
      id: changelog
      uses: janheinrichmerker/action-github-changelog-generator@v2.3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        output: CHANGELOG.md
        futureRelease: v${{ github.run_number }}
        issues: true
        pullRequests: true
        issuesWoLabels: true
        prWoLabels: true
        author: true
        compareLink: true
        breakingLabels: "breaking,breaking-change"
        enhancementLabels: "enhancement,feature,feat"
        bugLabels: "bug,fix,hotfix"
        securityLabels: "security,vulnerability"
        deprecatedLabels: "deprecated"
        removedLabels: "removed"
        unreleased: true
        unreleasedLabel: "Unreleased"
        headerLabel: "🤖 AI-Generated Changelog"
        configureSections: |
          {
            "sections": {
              "Breaking": {
                "prefix": "💥",
                "labels": ["breaking", "breaking-change"]
              },
              "Security": {
                "prefix": "🔒",
                "labels": ["security", "vulnerability"]
              },
              "Enhancements": {
                "prefix": "✨",
                "labels": ["enhancement", "feature", "feat"]
              },
              "Bug Fixes": {
                "prefix": "🐛",
                "labels": ["bug", "fix", "hotfix"]
              },
              "Documentation": {
                "prefix": "📚",
                "labels": ["documentation", "docs"]
              },
              "Style": {
                "prefix": "🎨",
                "labels": ["style", "ui", "design"]
              },
              "Refactoring": {
                "prefix": "♻️",
                "labels": ["refactor"]
              },
              "Performance": {
                "prefix": "⚡",
                "labels": ["performance", "perf"]
              },
              "Tests": {
                "prefix": "🧪",
                "labels": ["test", "testing"]
              },
              "CI/CD": {
                "prefix": "👷",
                "labels": ["ci", "cd", "workflow"]
              },
              "Build": {
                "prefix": "📦",
                "labels": ["build", "dependencies"]
              }
            }
          }
        
    - name: Create Git Tag
      id: create_tag
      if: github.ref == 'refs/heads/main'
      run: |
        # Get the latest tag
        LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
        echo "Latest tag: $LATEST_TAG"
        
        # Extract version numbers
        if [[ $LATEST_TAG =~ v([0-9]+)\.([0-9]+)\.([0-9]+) ]]; then
          MAJOR=${BASH_REMATCH[1]}
          MINOR=${BASH_REMATCH[2]}
          PATCH=${BASH_REMATCH[3]}
        else
          MAJOR=0
          MINOR=0
          PATCH=0
        fi
        
        # Increment patch version
        NEW_PATCH=$((PATCH + 1))
        NEW_VERSION="v$MAJOR.$MINOR.$NEW_PATCH"
        echo "New version: $NEW_VERSION"
        
        # Configure git
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # Create and push tag
        git tag -a "$NEW_VERSION" -m "Release $NEW_VERSION"
        git push origin "$NEW_VERSION"
        
        echo "✅ Tag $NEW_VERSION created and pushed"
        
        # Set output for next step
        echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
        
    - name: Download Chrome extension for release
      if: github.ref == 'refs/heads/main'
      uses: actions/download-artifact@v4
      with:
        name: C-3PO-chrome-v${{ github.run_number }}
        path: release-chrome
        
    - name: Download Firefox extension for release
      if: github.ref == 'refs/heads/main'
      uses: actions/download-artifact@v4
      with:
        name: C-3PO-firefox-v${{ github.run_number }}
        path: release-firefox
        
    - name: Create release ZIPs
      if: github.ref == 'refs/heads/main'
      run: |
        echo "📦 Creating release ZIPs..."
        
        # Create Chrome ZIP for release
        cd release-chrome
        zip -r ../C-3PO-chrome.zip .
        cd ..
        
        # Create Firefox ZIP for release  
        cd release-firefox
        zip -r ../C-3PO-firefox.zip .
        cd ..
        
        echo "✅ Release ZIPs created:"
        ls -la C-3PO-*.zip
        
        # Verify ZIP contents
        echo "🔍 Chrome ZIP contents:"
        unzip -l C-3PO-chrome.zip | head -10
        
        echo "🔍 Firefox ZIP contents:"  
        unzip -l C-3PO-firefox.zip | head -10
        
    - name: Create Release
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        files: |
          C-3PO-chrome.zip
          C-3PO-firefox.zip
        tag_name: ${{ steps.create_tag.outputs.new_version }}
        name: Release ${{ steps.create_tag.outputs.new_version }}
        body: |
          ## C-3PO ${{ steps.create_tag.outputs.new_version }}
          
          ${{ steps.changelog.outputs.changelog }}
          
          ### 🎉 Features
          - AI-powered translation using Google Gemini or OpenAI
          - Context menu integration for instant translations
          - Dark mode support for better user experience
          - Custom translation options and language pairs
          - Secure API key storage with encryption
          
          ### 🚀 Installation
          
          **Choose your browser:**
          
          **🟢 For Chrome/Edge/Brave:**
          1. Download the `C-3PO-chrome.zip` file from the Assets section above
          2. Extract the ZIP file
          3. Open Chrome and go to `chrome://extensions/`
          4. Enable "Developer mode" (top right toggle)
          5. Click "Load unpacked" and select the extracted folder
          
          **🦊 For Firefox:**
          1. Download the `C-3PO-firefox.zip` file from the Assets section above
          2. Extract the ZIP file  
          3. Open Firefox and go to `about:debugging`
          4. Click "This Firefox" 
          5. Click "Load Temporary Add-on..." and select the `manifest.json` file from the extracted folder
          
          ### ⚙️ Configuration
          - Open the extension and go to "AI Provider Settings"
          - Choose your preferred AI provider (Gemini or OpenAI)
          - Enter your API key
          - Customize context menu options as needed
          - Save the configuration
          
          ### 🔗 Quick Links
          - [GitHub Repository](https://github.com/NereaCassian/C-3PO)
          - [Issues](https://github.com/NereaCassian/C-3PO/issues)
          - [Documentation](https://github.com/NereaCassian/C-3PO#readme)
          
          ---
          
          **May the translations be with you!** 🌍✨
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
